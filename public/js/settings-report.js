$(document).ready(function() {
	$("#whiteLabelForm").validate();

	/*$("input[name='email']").on('change', function() {
		$('input[name="email"]').removeClass('error');
		$('#email-error').empty();
		$('#saveConfig').removeAttr('disabled'); // enable save button
	});
	$("input[name='time']").on('change', function() {
		$('input[name="time"]').removeClass('error');
		$('#time-error').empty();
		$('#saveConfig').removeAttr('disabled'); // enable save button
	});
	$("input[name='reseller']").on('change', function() {
		$('input[name="reseller"]').removeClass('error');
		$('#reseller-error').empty();
		$('#saveConfig').removeAttr('disabled'); // enable save button
	});
	$("input[name='detail']").on('change', function() {
		$('input[name="detail"]').removeClass('error');
		$('#detail-error').empty();
		$('#saveConfig').removeAttr('disabled'); // enable save button
	});*/
});